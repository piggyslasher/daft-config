import Config from 'daft-conf'

// process.chdir(__dirname)
xdescribe('Daft-conf integration test', () => {
  // it('should return the local config', async () => {
  //   console.log('tesasdt', await Config.create('config'))
  //   const conf = await Config.create('config')
  //   expect(conf.level).toBe('root')
  // })
})
